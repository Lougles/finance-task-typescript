export interface SocketData {
    ticker: string;
    exchange: string;
    price: string;
    change: string;
    change_percent: string;
    last_trade_time: string;
    dividend: string;
    yield: string;
}